export enum DataModelFieldType {
  Text = 'Text',
  Integer = 'Text',
  Float = 'Float',
  Boolean = 'Boolean',
}

export enum StorageType {
  Postgres = 'Postgres'
}

export enum StorageStatus {
  Pending = 'Pending',
  Ready = 'Ready'
}
