export enum DataModelFieldType {
  Text = 'Text',
  Integer = 'Integer',
  Float = 'Float',
  Boolean = 'Boolean',
}

export enum StorageType {
  Postgres = 'Postgres'
}

export enum StorageStatus {
  Pending = 'Pending',
  Ready = 'Ready'
}
